# miokit-plugin-development

MioKit **插件开发** Agent Skill（规范与 API 按主题合并在同一 reference）。

- **安装：** `npx skills add tiny-isle/MioKit.CodeAgent`（或 `--skill miokit-plugin-development`）
- **源码：** 本仓库 [`skills/miokit-plugin-development/`](./)

主题文档在 `references/*.md`（一类一文件，含用法与 API）。创建 / 打包 / 验包走同仓 MCP（`mcp/`）。
